# Copy to .agents/skills/notebooklm/.env (gitignored)

# Optional: override where browser profile and state are stored.
# Defaults store everything inside this skill's .data/ folder.
# NOTEBOOKLM_DATA_DIR=/Users/you/.notebooklm-data
